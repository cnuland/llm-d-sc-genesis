# Working memory (rewrite aggressively; this is not history)

Spec: 0.1-mvp
Current acceptance criterion: AC-004 pinned sensitivity model matches trusted
  reference embedding/ranking fixtures.

STATUS: ESCALATE — U-061 parity FAILS. Do not record GREEN-U061.md; do not weaken
the assertion; do not fix the implementation this turn. See ESCALATION below.

## ESCALATION (spec contradiction — STOP per AGENTS.md)

The reviewer's trusted reference `artifacts/ref-embedding.json` (first16 dims +
l2_norm, generated by `artifacts/u061_tight.sh` with transformers AutoModel +
masked mean pooling) was copied to `tests/fixtures/modelcar/golden-embedding.json`
and the new `#[ignore]` test `u061_pooling_output_matches_trusted_reference`
(asserting first16 within 1e-4 AND l2_norm within 1e-3) was added to
`src/embedding.rs`.

The test is RED for a real reason: the resident Rust Candle forward does NOT
match the trusted reference.
  - dim 0: got 0.030966658 vs want 0.09930510818958282 (diff 0.0683 > 1e-4)
  - got first16 [0.0309, -0.3689, -0.1736, ...] vs want
    [0.0993, 0.2062, 0.0782, ...] — substantively different embeddings
  - l2_norm: got 5.758741 vs want 5.833498 (diff ~0.0748 > 1e-3)
Command: `cargo test --locked -- --ignored u061` -> FAILED (0 passed; 1 failed).
Baseline `./hack/verify` exit 0 (u061/u062 ignored; 10 passed).

Why this is a contradiction: AC-004 requires the pinned sensitivity model's
embedding to match the trusted reference within tolerance. Tokenizer parity is
already proven exact (U-060, same golden input -> identical token IDs) and the
masked mean pooling is identical to the reference script, so the divergence
cannot be in tokenization or pooling. It is in the model forward itself
(weights/architecture). The deterministic test outranks the assumption that
parity already holds.

What I did NOT do (contract requires): did not write GREEN-U061.md (would be
falsified evidence), did not weaken the 1e-4/1e-3 assertions, did not alter the
implementation (the task is a narrow U-061 slice; a fix is out of scope and
un-authorized).

## Evidence
- tests/fixtures/modelcar/golden-embedding.json  (copied verbatim from artifacts/ref-embedding.json)
- src/embedding.rs  u061_pooling_output_matches_trusted_reference (#[ignore]) — RED
- specs/0.1-mvp/evidence/AC-004/GREEN-U061.md  NOT written (test is RED)

## Open uncertainty / flags for reviewer
- Root cause of the forward divergence is undiagnosed: resident
  `artifacts/models/sensitivity` safetensors/config vs whatever model the
  reviewer's reference loaded. The reference script `u061_tight.sh` calls
  `AutoTokenizer.from_pretrained("artifacts/models/sensitivity")` but that dir
  has NO tokenizer.json, so the exact provenance of the reference embedding
  needs review.
- Next step for a human: reconcile which model/weights the trusted reference
  was produced from, or fix the resident forward to match it, then re-run U-061.
