# Working memory (rewrite aggressively; this is not history)

Spec: 0.1-mvp
Current acceptance criterion: AC-004 pinned sensitivity model matches trusted
  reference embedding/ranking fixtures.
Slice completed this turn: U-067 (golden pipeline fixture) + whole-criterion
  AC-004 GREEN.md.

## U-067 — GREEN (this turn)

### What landed
- New fixture `tests/fixtures/modelcar/golden-ranking.json` (golden pipeline
  fixture), generated by `artifacts/gen_golden_ranking.py` via python3:
  `SentenceTransformer("artifacts/models/sensitivity").encode("this is a golden
  sensitivity input")`, cosine-ranked against the 4 SYNTHETIC prototypes
  (proto-a..proto-d from `synthetic-prototypes.json`, label
  `synthetic_for_mechanics_only`). Provenance embedded in the fixture; notes
  the prototypes are synthetic mechanics-only, NOT a real taxonomy. Reference
  order: [proto-a, proto-d, proto-c, proto-b].
- `src/embedding.rs`: new `#[ignore]`d test
  `u067_golden_fixture_ranking_matches_reference` + `synthetic_prototypes()`
  helper. Composes existing `Embedder::embed` + `crate::ranker::cosine_rank`;
  asserts exact order and scores within 1e-4 of the fixture.
- Evidence: specs/0.1-mvp/evidence/AC-004/RED-U067.md + GREEN-U067.md. RED was
  proven non-vacuously by temporarily swapping the fixture's top two ids and
  showing the order assertion fails, then restoring.

### Suites (all GREEN)
- ./hack/test-impact: UNKNOWN SURFACE (src/embedding.rs not in map) -> FULL
  SUITE; `./hack/test-all` green (12 passed, 4 ignored).
- ./hack/test-parity: green (4 passed — U-061/062/063/067 ignored model tests).
- ./hack/verify: GREEN exit 0 (fmt, clippy -D warnings, build --locked,
  `cargo test --workspace --all-features --locked`).

### Whole-criterion GREEN.md written
- specs/0.1-mvp/evidence/AC-004/GREEN.md summarizes all 8 slices U-060..U-067:
  U-060/064/065/066 via plain `cargo test`; U-061/062/063/067 via
  `./hack/test-parity`. Notes I-020..I-025 remain for the integration
  environment per the test plan (not proven by the unit slice).

### Worktree
- SHA 7736ea9 (uncommitted changes). git status: M src/lib.rs, M src/embedding.rs,
  M .agent/state/current.md; ?? src/ranker.rs, ?? tests/fixtures/modelcar/
  synthetic-prototypes.json, ?? tests/fixtures/modelcar/golden-ranking.json,
  ?? artifacts/gen_golden_ranking.py, ?? AC-004 evidence md files (RED/GREEN for
  U-064/065/067, whole GREEN.md). No commits/pushes.

## Open items / flags for reviewer
- Prior note that U-061 was RED/ESCALATED is now STALE: the parity suite passes
  U-061 with the current golden-embedding.json (l2_norm 0.99999998) + the U-063
  normalization fix. Reconciliation appears resolved; reviewer should confirm.
- Next step for a human: I-020..I-025 integration slices (real artifact loads,
  public-like/regulated-like/never-egress-like/adversarial golden fixtures,
  Rust-vs-pinned-Python tolerance) per the test plan, in the integration
  environment.
