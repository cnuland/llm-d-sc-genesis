artifacts/
target/
node_modules/
*.log
.DS_Store

# Training pipeline outputs: regenerable from training/sdg_llm.py + verify.py.
# The generated corpora and fine-tuned weights are large and are reproduced by
# the pipeline, so only the pipeline itself is version-controlled.
training/data/
training/models/

# direnv environment files. These hold live credentials (ANTHROPIC_API_KEY and
# similar) and must never be committed. Ignored rather than merely untracked,
# because "untracked" is one `git add -A` away from a leaked key.
.envrc
.envrc.local
.direnv/
